<?xml version="1.0" encoding="utf-8" ?>
<xct:Popup xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
           xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
           x:Class="DailyBudgetMAUIApp.Handlers.PopUpPage"
           xmlns:xct="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
           CanBeDismissedByTappingOutsideOfPopup="False"
           Color="Transparent">
    <Grid ColumnDefinitions="*" RowDefinitions="*" BackgroundColor="Transparent" x:Name="MainGrid">
        <Border HeightRequest="150" x:Name="border" Grid.Row="0" Grid.Column="0" Style="{DynamicResource DashboardMainContent}" StrokeThickness="1" VerticalOptions="Center" HorizontalOptions="Center" StrokeShape="RoundRectangle 10">
            <VerticalStackLayout>
                <Label Text="Please wait" FontAttributes="Bold" FontSize="Medium" VerticalOptions="Center" HorizontalOptions="Center" FontFamily="OpenSansSemibold" TextColor="{DynamicResource Primary}" Margin="0,10,0,0"/>
                <ActivityIndicator IsRunning="True" IsVisible="True" VerticalOptions="Center" HorizontalOptions="Center" Margin="0,40,0,20" Color="{DynamicResource Primary}" Scale="1.2"/>
            </VerticalStackLayout>
        </Border>
    </Grid>
</xct:Popup>