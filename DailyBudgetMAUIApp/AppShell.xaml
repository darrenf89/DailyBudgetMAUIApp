<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="DailyBudgetMAUIApp.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:DailyBudgetMAUIApp"
    xmlns:Pages="clr-namespace:DailyBudgetMAUIApp.Pages"
    >

    <ShellContent
        FlyoutItemIsVisible="False" 
        Shell.FlyoutBehavior="Disabled"
        Shell.TabBarIsVisible="False"
        Shell.NavBarIsVisible="false"
        Title="Landing Page"
        ContentTemplate="{DataTemplate Pages:LoadUpPage}"
        Route="LoadUpPage" />

    <ShellContent
        FlyoutItemIsVisible="False" 
        Shell.FlyoutBehavior="Disabled"
        Shell.TabBarIsVisible="False"
        Shell.NavBarIsVisible="false"
        Title="Login Page"
        ContentTemplate="{DataTemplate Pages:LogonPage}"
        Route="LogonPage" />

    <ShellContent
        FlyoutItemIsVisible="False" 
        Shell.FlyoutBehavior="Disabled"
        Shell.TabBarIsVisible="False"
        Shell.NavBarIsVisible="false"
        Title="Register Page"
        ContentTemplate="{DataTemplate Pages:RegisterPage}"
        Route="RegisterPage" />

</Shell>
