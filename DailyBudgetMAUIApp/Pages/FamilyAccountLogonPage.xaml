<?xml version="1.0" encoding="utf-8" ?>
<local:BasePage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="DailyBudgetMAUIApp.Pages.FamilyAccountLogonPage"
             xmlns:local="clr-namespace:DailyBudgetMAUIApp"
             xmlns:handlers="clr-namespace:DailyBudgetMAUIApp.Handlers"
             xmlns:helpers="clr-namespace:DailyBudgetMAUIApp.Helpers"
             xmlns:xct="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"             
             Shell.FlyoutBehavior="Disabled"
             Shell.TabBarIsVisible="False"
             Shell.NavBarIsVisible = "False" >
    <Grid>
        <Image Source="logonbackgroundorange.jpg"
               Aspect="AspectFill"
               Grid.Row="0"
               Grid.Column="0" />
        <ScrollView x:Name="LP">
            <Grid>
                <VerticalStackLayout VerticalOptions="Start">
                    <Border Style="{StaticResource PictureFrame300Rectangle}" Margin="0,40,0,140">
                        <Image Source="colorlogonobackground.png" ></Image>
                    </Border>
                    <Border Style="{StaticResource FrameWhiteStandardContent}">
                        <VerticalStackLayout HorizontalOptions="Fill" >                       
                            <Label Text="Welcome to family accounts!" 
                                TextColor="{DynamicResource Primary}" 
                                FontSize="{Binding Source=Large, Converter={StaticResource StringToSizeConverter}}" 
                                HorizontalOptions="Center" 
                                FontAttributes="Bold"
                                Margin="0,20,0,2"/>
                            <Label Text="Login and continue the daily grind to freedom!" 
                                TextColor="{DynamicResource Primary}" 
                                FontSize="{Binding Source=Small, Converter={StaticResource StringToSizeConverter}}" 
                                HorizontalOptions="Center" />
                            <Border Stroke="{DynamicResource Tertiary}" 
                                WidthRequest="280"
                                HeightRequest="2"
                                Margin="0,20,0,30" 
                                StrokeThickness="1"/>
                            <VerticalStackLayout>
                                <Border Padding="10,5,10,5" Margin="0,5,0,15" IsVisible="{Binding EmailValidatedSuccess}" Background="{StaticResource SuccessLight}" StrokeShape="RoundRectangle 3" Stroke="{StaticResource SuccessBrush}" StrokeThickness="1">
                                    <Label LineBreakMode="WordWrap" VerticalOptions="Center">
                                        <Label.FormattedText>
                                            <FormattedString>
                                                <Span Text="Congrats!" TextColor="{StaticResource Success}" FontAttributes="Bold"/>
                                                <Span Text=" You have verified your email, you can now login to your account" TextColor="{StaticResource Success}"/>
                                            </FormattedString>
                                        </Label.FormattedText>
                                    </Label>
                                </Border>
                                <Border Padding="10,5,10,5" Margin="0,5,0,15" IsVisible="{Binding AccountCreationSuccess}" Background="{StaticResource SuccessLight}" StrokeShape="RoundRectangle 3" Stroke="{StaticResource SuccessBrush}" StrokeThickness="1">
                                    <Label LineBreakMode="WordWrap" VerticalOptions="Center">
                                        <Label.FormattedText>
                                            <FormattedString>
                                                <Span Text="Congrats!" TextColor="{StaticResource Success}" FontAttributes="Bold"/>
                                                <Span Text=" You have verified your email, and set up your new account. Log in to get budgeting" TextColor="{StaticResource Success}"/>
                                            </FormattedString>
                                        </Label.FormattedText>
                                    </Label>
                                </Border>
                                <Border Padding="10,5,10,5" Margin="0,5,0,15" IsVisible="{Binding AccountCreationFail}" Background="{StaticResource DangerLight}" StrokeShape="RoundRectangle 3" Stroke="{StaticResource DangerBrush}" StrokeThickness="1">
                                    <Label LineBreakMode="WordWrap" VerticalOptions="Center">
                                        <Label.FormattedText>
                                            <FormattedString>
                                                <Span Text="Opps that didnt work!" TextColor="{StaticResource Danger}" FontAttributes="Bold"/>
                                                <Span Text=" Please try again if you still want to set up your account" TextColor="{StaticResource Danger}"/>
                                            </FormattedString>
                                        </Label.FormattedText>
                                    </Label>
                                </Border>
                                <Border Padding="10,5,10,5" Margin="0,5,0,15" IsVisible="{Binding ResetPasswordSuccess}" Background="{StaticResource SuccessLight}" StrokeShape="RoundRectangle 3" Stroke="{StaticResource SuccessBrush}" StrokeThickness="1">
                                    <Label LineBreakMode="WordWrap" VerticalOptions="Center">
                                        <Label.FormattedText>
                                            <FormattedString>
                                                <Span Text="Congrats!" TextColor="{StaticResource Success}" FontAttributes="Bold"/>
                                                <Span Text=" You have reset your password, you can now loging" TextColor="{StaticResource Success}"/>
                                            </FormattedString>
                                        </Label.FormattedText>
                                    </Label>
                                </Border>
                                <Border Padding="10,5,10,5" Margin="0,5,0,15" IsVisible="{Binding ResetPasswordFail}" Background="{StaticResource DangerLight}" StrokeShape="RoundRectangle 3" Stroke="{StaticResource DangerBrush}" StrokeThickness="1">
                                    <Label LineBreakMode="WordWrap" VerticalOptions="Center">
                                        <Label.FormattedText>
                                            <FormattedString>
                                                <Span Text="Opps that didnt work!" TextColor="{StaticResource Danger}" FontAttributes="Bold"/>
                                                <Span Text=" Please try again if you want to reset your password" TextColor="{StaticResource Danger}"/>
                                            </FormattedString>
                                        </Label.FormattedText>
                                    </Label>
                                </Border>
                            </VerticalStackLayout>
                            <HorizontalStackLayout HorizontalOptions="Fill" VerticalOptions="Start">
                                <VerticalStackLayout Margin="0,0,0,15">
                                    <Border Style="{StaticResource FrameLogonStandardContent}">
                                        <handlers:BorderlessEntry Text="{Binding Email}"
                                       Placeholder="Enter an Email address!"
                                       Keyboard="Text" 
                                       Style="{StaticResource LogonEntry}"
                                       MaxLength="40">
                                            <Entry.Behaviors>
                                                <xct:EmailValidationBehavior  x:Name="EmailValid"
                                            Flags="ValidateOnUnfocused,ForceMakeValidWhenFocused"
                                            IsValid="{Binding EmailValid}" 
                                            BindingContext="{Binding BindingContext, Source={x:Reference LP}, x:DataType=FamilyAccountLogonPageViewModel}" />
                                                <xct:TextValidationBehavior x:Name="EmailRequired"
                                            MinimumLength="1"
                                            Flags="ValidateOnUnfocused,ForceMakeValidWhenFocused"
                                            IsValid="{Binding EmailRequired}"
                                            BindingContext="{Binding BindingContext, Source={x:Reference LP}, x:DataType=FamilyAccountLogonPageViewModel}" />
                                            </Entry.Behaviors>
                                        </handlers:BorderlessEntry>
                                    </Border>
                                    <HorizontalStackLayout 
                                IsVisible="{Binding EmailRequired, Converter={StaticResource BoolConverter}}"                                                 
                                Margin="20,0,0,0">
                                        <Image>
                                            <Image.Source>
                                                <FontImageSource 
                                            FontFamily="MaterialDesignIcons"                                                         
                                            Glyph="{x:Static helpers:MaterialDesignIconsFonts.Error}"                                                         
                                            Size="15"                                                          
                                            Color="{DynamicResource Danger}"/>
                                            </Image.Source>
                                        </Image>
                                        <Label Text="Please let us know your email, pretty please!"
                                   Style="{StaticResource ErrorMessage}"/>
                                    </HorizontalStackLayout>
                                    <HorizontalStackLayout                                    
                                IsVisible="{Binding EmailValid, Converter={StaticResource BoolConverter}}"
                                Margin="20,0,0,0">
                                        <Image>
                                            <Image.Source>
                                                <FontImageSource 
                                            FontFamily="MaterialDesignIcons"                                                         
                                            Glyph="{x:Static helpers:MaterialDesignIconsFonts.Error}"                                                         
                                            Size="15"                                                          
                                            Color="{DynamicResource Danger}"/>
                                            </Image.Source>
                                        </Image>
                                        <Label Text="You have to enter a valid email ... or else!!" 
                                   Style="{StaticResource ErrorMessage}"/>
                                    </HorizontalStackLayout>
                                </VerticalStackLayout>
                                <Image Margin="15,0,0,15">
                                    <Image.Source>
                                        <FontImageSource FontFamily="MaterialDesignIcons"
                                    Glyph="{x:Static helpers:MaterialDesignIconsFonts.Email}"
                                    Size="25" 
                                    Color="{DynamicResource Primary}"/>
                                    </Image.Source>
                                </Image>
                            </HorizontalStackLayout>
                            <HorizontalStackLayout HorizontalOptions="Fill" VerticalOptions="Start">
                                <VerticalStackLayout Margin="0,0,0,15">
                                    <Border Style="{StaticResource FrameLogonStandardContent}">
                                        <handlers:BorderlessEntry x:Name="entPassword" Text="{Binding Password}"
                                   Placeholder="Please enter a password!"
                                   Keyboard="Text"
                                   IsPassword="True"
                                   Style="{StaticResource LogonEntry}">
                                            <Entry.Behaviors>
                                                <xct:TextValidationBehavior  x:Name="PasswordRequired"
                                            MinimumLength="1"
                                            Flags="ValidateOnUnfocused,ForceMakeValidWhenFocused"
                                            IsValid="{Binding PasswordRequired}"
                                            BindingContext="{Binding BindingContext, Source={x:Reference LP}, x:DataType=FamilyAccountLogonPageViewModel}" />
                                            </Entry.Behaviors>
                                        </handlers:BorderlessEntry>
                                    </Border>
                                    <HorizontalStackLayout 
                                IsVisible="{Binding PasswordRequired, Converter={StaticResource BoolConverter}}"
                                Margin="20,0,0,0">
                                        <Image>
                                            <Image.Source>
                                                <FontImageSource 
                                            FontFamily="MaterialDesignIcons"                                                         
                                            Glyph="{x:Static helpers:MaterialDesignIconsFonts.Error}"                                                         
                                            Size="15"                                                          
                                            Color="{DynamicResource Danger}"/>
                                            </Image.Source>
                                        </Image>
                                        <Label Text="Come on ... you must know you need a password!"                                   
                                       Style="{StaticResource ErrorMessage}"/>
                                    </HorizontalStackLayout>
                                </VerticalStackLayout>
                                <Image Margin="15,0,0,15">
                                    <Image.Source>
                                        <FontImageSource FontFamily="MaterialDesignIcons"
                                    Glyph="{x:Static helpers:MaterialDesignIconsFonts.Vpn_key}"
                                    Size="25" 
                                    Color="{DynamicResource Primary}"/>
                                    </Image.Source>
                                </Image>
                            </HorizontalStackLayout>
                            <VerticalStackLayout Margin="0,0,0,5">
                                <HorizontalStackLayout HorizontalOptions="End" VerticalOptions="Center">
                                    <Label Text="Reset Password"
                                        TextColor="{DynamicResource Primary}"
                                        TextDecorations="Underline"
                                        FontAttributes="Bold"
                                        FontSize="{Binding Source=Caption, Converter={StaticResource StringToSizeConverter}}">
                                        <Label.GestureRecognizers>
                                            <TapGestureRecognizer Command="{Binding ResetPasswordCommand}"/>
                                        </Label.GestureRecognizers>
                                    </Label>
                                </HorizontalStackLayout>
                            </VerticalStackLayout>
                            <HorizontalStackLayout HorizontalOptions="Fill" VerticalOptions="Center" HeightRequest="20" Margin="0,0,0,10">
                                <CheckBox IsChecked="{Binding RememberMe}"/>
                                <Label Text="Remember Me!"
                                   VerticalOptions="Center"
                                   TextColor="{DynamicResource Gray400}"
                                   FontSize="{Binding Source=Caption, Converter={StaticResource StringToSizeConverter}}"
                                   Padding="0" />
                            </HorizontalStackLayout>
                            <Button Text="Sign in"
                                TextColor="White"
                                Margin="60,20,60,0"
                                Style="{StaticResource PrimaryButton}" 
                                Command="{Binding LoginCommand}">
                                <Button.ImageSource>
                                    <FontImageSource 
                                     FontFamily="MaterialDesignIcons"
                                     Glyph="{x:Static helpers:MaterialDesignIconsFonts.Lock_open}"
                                     Size="20" />
                                </Button.ImageSource>
                            </Button>
                            <VerticalStackLayout Margin="60,20,60,0">
                                <Button Text="Complete Set Up"
                                TextColor="White" 
                                Style="{StaticResource PrimaryButton}" 
                                BackgroundColor="{StaticResource Success}"
                                Command="{Binding RegisterCommand}">
                                    <Button.ImageSource>
                                        <FontImageSource 
                                     FontFamily="MaterialDesignIcons"
                                     Glyph="{x:Static helpers:MaterialDesignIconsFonts.App_registration}"
                                     Size="20" />
                                    </Button.ImageSource>
                                </Button>
                            </VerticalStackLayout>
                            <HorizontalStackLayout HorizontalOptions="Center" VerticalOptions="Center" Margin="0,20,0,20">
                                <Label Text="Looking for your regular account, "
                                FontAttributes="Bold" 
                                FontSize="{Binding Source=Small, Converter={StaticResource StringToSizeConverter}}">
                                </Label>
                                <Label Text="Click Here!"
                                TextColor="{DynamicResource Primary}"
                                TextDecorations="Underline"
                                FontAttributes="Bold"
                                FontSize="{Binding Source=Small, Converter={StaticResource StringToSizeConverter}}">
                                    <Label.GestureRecognizers>
                                        <TapGestureRecognizer Command="{Binding NavigateNormalSignPageCommand}"/>
                                    </Label.GestureRecognizers>
                                </Label>
                            </HorizontalStackLayout>
                        </VerticalStackLayout>
                    </Border>
                </VerticalStackLayout>
            </Grid>
        </ScrollView>
    </Grid>
</local:BasePage>